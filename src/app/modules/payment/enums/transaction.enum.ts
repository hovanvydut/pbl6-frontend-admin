export enum TransactionType {
    Recharge = 1,
    Payment = 2,
}

export enum TransactionStatus {
    Success = 'Giao dịch thành công',
    Failed = 'Không giao dịch',
}

export enum PostActionType {
    create_post = 'Tạo bài viết',
    uptop_post = 'Đẩy tin ưu tiên'
}